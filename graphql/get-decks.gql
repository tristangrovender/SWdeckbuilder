query GetDecks($authorId: ID) {
  decks(authorId: $authorId) {
    id
    title
    createdAt
    published
    side
    ratings {
      id
      rating
    }
    author {
      id
      username
    }
  }
}
